<header id="header" class="header d-flex align-items-center sticky-top">
	<div class="container-fluid container-xl position-relative d-flex align-items-center">

		{% set isAdmin = is_granted('ROLE_ADMIN') %}
		{% set isOtherRole = is_granted('ROLE_USER') %}

		{# {% set isOtherRole = is_granted('ROLE_USER') or is_granted('ROLE_PATIENT_ADMIN') %} #}


		<a href="/" class="logo d-flex align-items-center me-auto">

			<h1 class="sitename" style="color: #0fb706; font-style: italic; font-family: cursive;">
				Beocler</h1>
		</a>

		<nav id="navmenu" class="navmenu">
			<ul>
				{% if app.user %}
					{% if not isAdmin %}
						<li>
							<a href="{{ path('test_index')}}" class="{% if '/test/' in app.request.uri %}active{% endif %}">Passez des épreuves</a>


						</li>
						<li>
							<a href="/patient" class="{% if '/patient/' in app.request.uri %}active{% endif %}">Gestion des patients</a>


						</li>

						<li>
							<a href="{{ path('app_evaluation_evaluations')}}" class="{% if '/evaluation' in app.request.uri %}active{% endif %}">Mes evaluations</a>


						</li>

					{% else %}
						<li>
							<a href="#" class="active">Accueil</a>
						</li>
						<li>
							<a href="#about-2">A propos</a>
						</li>

						<li>
							<a href="#footer">Contact</a>
						</li>

					{% endif %}

				{% else %}

					<li>
						<a href="#" class="active">Accueil</a>
					</li>
					<li>
						<a href="#about-2">A propos</a>
					</li>

					<li>
						<a href="#footer">Contact</a>
					</li>
				{% endif %}

				<li class="dropdown">
					<a href="">
						<button class="btn-inscrir " href="" style="border: 0px">Espace proffessionnel</button>
					</a>

					{% if app.user %}
						{% if app.user.getIsVerified() and app.user.getInscriptionStatus() == 'valide' %}
							<ul>
								<li>
									<a href="{{ path('users_index') }}">Mon compte</a>
								</li>
								<li>
									<a href="{{ path('app_logout') }}">Deconnecter</a>
								</li>
							</ul>
						{% else %}
							<ul>
								<li><ahref="{{path('app_logout')}}">Deconnecter</a></li>
							</ul>
						{% endif %}
					{% else %}
						<ul>
							<li><a href="/login">Connexion</a></li>
							<li><a href="/register">Inscription</a></li>
						</ul>
					{% endif %}

				</li>


			</ul>
			<i class="mobile-nav-toggle d-xl-none bi bi-list"></i>
		</nav>


	</div>
</header>
